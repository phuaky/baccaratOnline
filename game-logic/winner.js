//DETERMINE WHO WINS
//BANKER DECIDES WHEN TO CHECK if he only has 2 cards
//ALL OPEN if banker has 3 cards
//When got natural or special handtype

function winner(player, banker) {
  //check handtype
  if (!player.handType) {

  } else if (player.valueOfHand > banker.valueOfHand)
}
